const NoResultFound = ({ text }) => {
  return (
    <div className="text-center py-12">
      <p className="text-gray-600 text-lg font-medium lg:tex-2xl">{text}</p>
    </div>
  )
}
export default NoResultFound
